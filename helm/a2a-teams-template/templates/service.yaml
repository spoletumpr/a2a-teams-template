apiVersion: v1
kind: Service
metadata:
  name: {{ include "a2a-teams-template.fullname" . }}
  labels:
    {{- include "a2a-teams-template.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "a2a-teams-template.selectorLabels" . | nindent 4 }}
